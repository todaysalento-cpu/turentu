// turentu-frontend/next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  turbopack: {
    root: './', // questa cartella è la radice del frontend
  },
};

module.exports = nextConfig;
